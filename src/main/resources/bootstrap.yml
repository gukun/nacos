server:
  port: 8762
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
#        group: DEFAULT_GROUP
#        prefix: ace-auth
  profiles:
    active: dev